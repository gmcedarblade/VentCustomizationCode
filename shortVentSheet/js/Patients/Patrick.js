// Patient Info Table Info
var name = "Patrick A. Armstrong";
var dob = "11/16/20XX";
var mrNum = "1116";
var allergy = "NDKA";
var weight = "109 kg";
var height = "177.5 cm";

// Shortened Ventilator Sheet Settings
var minSetRate = 0;    // 0
var maxSetRate = 500;   // 80

var minTotalRate = 0;  // 0
var maxTotalRate = 500; // 80

var minSetVt = 0;    // 0
var maxSetVt = 5000; // 2000

var minExpireVt = 0;     // 0
var maxExpireVt = 5000;  // 2000

var minExhaleVe = 0;   // 0
var maxExhaleVe = 500;  // 50

var minO2Pcnt = 0;   // 0
var maxO2Pcnt = 200; // 100

var minSetPeep = 0;  // 0
var maxSetPeep = 400; // 40

var minPip = 0;  // 0
var maxPip = 800; // 80

var minPlateau = 0;  // 0
var maxPlateau = 800; // 0

var minMap = 0;  // 0
var maxMap = 800; // 80

var minPeakFlow = 0;   // 0
var maxPeakFlow = 500; // 200

var minClStatic = 42.9;   // 0
var maxClStatic = 43; // 150

var minClDynamic = 27.7;    // 0
var maxClDynamic = 27.9;  // 150

var minRaw = 10.5;  // 0
var maxRaw = 10.7; // 50

var minInspTime = 0; // 0
var maxInspTime = 20; // 5

var minIeRatioLeft = 1;  // 1
var maxIeRatioLeft = 1;  // 3

var minIeRatioRight = 4;   // 1
var maxIeRatioRight = 4.2;  // 15

// Messages
var messageSuccess = "Entries correct.";
var messageFailure = "Please check the accuracy of your entries and try again.";

// Suggested Ventilator Settings 

var minRateSetting = 10;
var  maxRateSetting = 16;

var minVtSetting = 500;
var maxVtSetting = 670;

var minFio2Setting = 50;
var maxFio2Setting = 100;

var minPeepSetting = 5;
var maxPeepSetting = 7;

var messageSuccessVentSetting = 'Success!';
var messageFailureVentSetting = 'Try again.';